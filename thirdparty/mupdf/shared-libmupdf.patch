--- a/Makefile
+++ b/Makefile
@@ -360,6 +360,11 @@ $(THIRD_LIB) : $(THIRD_OBJ)
 $(THREAD_LIB) : $(THREAD_OBJ)
 $(PKCS7_LIB) : $(PKCS7_OBJ)
 
+MUPDF_SHLIB = $(OUT)/libmupdf.so
+$(MUPDF_SHLIB): $(MUPDF_LIB) $(THIRD_LIB) $(LIBS)
+	$(QUIET_LINK) $(CC) $(LINK_OPTS) $(LIBS) -o $@
+shared: $(MUPDF_SHLIB)
+
 INSTALL_LIBS := $(MUPDF_LIB) $(THIRD_LIB)
 
 # --- Tools and Apps ---
